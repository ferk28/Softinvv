<a href="{{ route('area.edit', $id) }}" class="btn btn-primary btn-md">Editar</a>
{{-- <form action="{{route('area.destroy',['areas'=>$area->$id])}}" method="POST">
@method('DELETE')
@csrf
<a href="{{route('area.destroy',['area'=>$area->$id])}}" class="btn btn-danger btn-md">Eliminar</a>
<button class="btn btn-danger" type="submit">Eliminar</button>
</form> --}}